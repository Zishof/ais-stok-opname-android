<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Fitur "Update Otomatis" (mirip Windows Update): unduh APK rilis baru dari GitHub lalu buka
         installer sistem Android (lihat ApkInstallerPlugin.java + updater.js). Android 8+ (API 26+)
         mewajibkan izin ini per-aplikasi utk memicu instalasi APK dari luar Play Store; tanpa ini
         sistem menolak diam-diam/menampilkan halaman "izinkan sumber tidak dikenal" yang membingungkan. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Scan barcode via kamera (fallback saat device tak punya scanner/infrared hardware), lihat
         JavaDoc app.js bagian "Scan kamera (ML Kit)". Plugin capacitor-mlkit/barcode-scanning
         SUDAH menyertakan izin ini di manifest bawaannya sendiri (merge otomatis Gradle); baris
         di bawah ini murni dokumentasi eksplisit, aman dideklarasikan dua kali (deduplikasi Gradle). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
